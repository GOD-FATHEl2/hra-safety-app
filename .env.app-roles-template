# Azure AD App Registration Environment Variables for App Roles
# Copy this to .env and fill in your Azure AD application details

# === AZURE AD APPLICATION REGISTRATION ===
# Get these values from your Azure AD App Registration

# Application (client) ID from Azure portal
AZURE_CLIENT_ID=your-application-client-id-here

# Client secret from Azure portal (Certificates & secrets)
AZURE_CLIENT_SECRET=your-client-secret-here

# Directory (tenant) ID from Azure portal
AZURE_TENANT_ID=your-tenant-id-here

# === JWT CONFIGURATION ===
# Secret for signing JWT tokens (change this to a secure random value)
JWT_SECRET=your-super-secure-jwt-secret-here

# === SESSION CONFIGURATION ===
# Secret for session management (change this to a secure random value)
SESSION_SECRET=your-super-secure-session-secret-here

# === ENVIRONMENT CONFIGURATION ===
# Set to 'production' when deploying to Azure
NODE_ENV=development

# === DATABASE CONFIGURATION ===
# SQLite database file path (relative to project root)
DATABASE_PATH=./hogrisk.db

# === REDIRECT URIS (for development/production) ===
# Development redirect URI
REDIRECT_URI_DEV=http://localhost:8080/auth/callback

# Production redirect URI (update with your Azure App Service URL)
REDIRECT_URI_PROD=https://your-app-name.azurewebsites.net/auth/callback

# === LOGGING ===
# Set to 'true' to enable verbose logging
ENABLE_VERBOSE_LOGGING=false

# === AZURE AD APP ROLES CONFIGURATION ===
# Your app roles are configured in Azure AD with these values:
# - admin (HRA Admin)
# - superintendent (HRA Superintendent) 
# - arbetsledare (HRA Arbetsledare)
# - supervisor (HRA Supervisor)
# - underhall (HRA Underhall)

# === IMPORTANT SECURITY NOTES ===
# 1. NEVER commit this file with real values to version control
# 2. Use Azure Key Vault for production secrets
# 3. Rotate secrets regularly
# 4. Use different secrets for development and production